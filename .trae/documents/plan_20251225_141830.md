1. 分析问题：验证码图片显示大小写混合，但SpecCaptcha.text()方法返回小写，导致Redis中存储的是小写
2. 查看SpecCaptcha类的API，确认是否有设置返回原始大小写的方法
3. 解决方案：

   * 方案一：修改getCode()方法，使用toUpperCase()或toLowerCase()统一验证码大小写，便于前端验证

   * 方案二：如果SpecCaptcha支持，设置返回原始大小写，保持图片和Redis中存储的一致性
4. 实施计划：

   * 检查SpecCaptcha类是否有相关配置方法

   * 修改getCode()方法，确保验证码在图片和Redis中保持一致

