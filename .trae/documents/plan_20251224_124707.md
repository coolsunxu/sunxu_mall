## 实现计划

1. **定位文件位置**：在 UserWebEntityMapper.xml 文件中添加新的 select 语句
2. **设置语句属性**：
   - id: "findByUserName"（与方法名匹配）
   - parameterType: "java.lang.String"（参数类型为字符串）
   - resultMap: "BaseResultMap"（使用现有的结果映射）
3. **编写 SQL 语句**：
   - 使用现有的 Base_Column_List 包含所有列
   - 表名为 sys_user
   - where 条件：user_name = #{userName,jdbcType=VARCHAR}
4. **添加到合适位置**：将新的 select 语句添加到现有的 select 语句附近，保持文件结构清晰

## 预期结果

完成后，UserWebEntityMapper.xml 文件将包含一个新的 select 语句，实现根据用户名查询用户信息的功能，与 UserWebEntityMapper.java 中的 findByUserName 方法对应。